# @stratum/shared

Shared types + constants. Imported by every other workspace.

## Exports

- `types` — `AgentMetadata`, `InferenceReceipt`, `RevenueSnapshot` (canonical schemas, see `docs/01-architecture.md` §6)
- `constants` — chain ids, hero agent identity, share supply defaults
