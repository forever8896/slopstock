/**
 * Viem helpers — chain configs, contract clients, transaction helpers.
 *
 * Planned exports:
 *   chains.zg, chains.baseSepolia, chains.sepolia
 *   readAgent(client, ticker) → on-chain reads
 *   buildMintTx(...)
 *   buildFractionalizeTx(...)
 *   buildSubscribeFlow(...)   — composes pay-with-any-token + authorizeUsage
 */

export {};
