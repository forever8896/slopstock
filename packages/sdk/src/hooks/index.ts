/**
 * Wagmi hooks for the Stratum frontend.
 *
 * Planned exports (not yet implemented):
 *   useAgent(ticker)           — full agent profile (chain + indexer)
 *   useShareBalance(addr, t)   — shareholder balance for an agent
 *   useRevenue(ticker, range)  — revenue series for a window
 *   useBuyShares(ticker)       — IPO/secondary buy mutation
 *   useSubscribe(ticker)       — pay + authorize + invoke
 *   usePostBid(ticker)         — acquisition bid mutation
 *   useAcceptBid(ticker)       — accept (operator only)
 *   useClaimDividends(ticker)  — pull-pattern claim
 *
 * Spec: docs/07-frontend.md.
 */

export {};
