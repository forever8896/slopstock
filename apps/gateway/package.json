{
  "name": "@stratum/gateway",
  "version": "0.0.1",
  "private": true,
  "description": "ENS CCIP-Read gateway worker — serves *.stratum.eth resolutions.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  }
}
